spring:
  application:
    name: servicetwo
server:
  port: ${port:8081} #java -jar target/restone-0.0.1-SNAPSHOT.jar --port=8090
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    
